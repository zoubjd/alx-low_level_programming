#include <unistd.h>
#include "main.h"
/**
 * mul - Entry point
 * Description: 'the program outputs multiplication of anb'
 * @a: is the first int that's multipled
 * @b: is the second int that's multipled
 * Return: (Success)
 */

int mul(int a, int b)
{
return (a * b);
}

