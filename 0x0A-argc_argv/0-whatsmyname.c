#include <stdio.h>

/**
 * main - check the code
 * description: the program prints its name
 * @argc: the array
 * @argv: the content
 * Return: Always 0.
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
