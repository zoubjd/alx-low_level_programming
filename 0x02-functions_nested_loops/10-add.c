#include <unistd.h>
#include "main.h"
/**
 * add - Entry point
 * Description: 'the program outputs the addition of two ints'
 * @m: is the first int
 * @n: is the second int
 * Return: Always 0 (Success)
 */
int add(int m, int n)
{
int result = 0;
result = m + n;
return (result);
}

